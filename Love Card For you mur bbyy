<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Happy 4 months 💋🫶💘💓❤️‍🩹 — For Deha bbyy</title>
<style>
  :root{
    --bg1:#071029; --bg2:#0f1724;
    --accent:#ff5d8f; --accent-2:#ff9cc8; --muted:rgba(230,238,248,0.68);
  }
  *{box-sizing:border-box;font-family:system-ui,-apple-system,"Segoe UI",Roboto,Arial}
  html,body{height:100%;margin:0;color:#e9f0fb}
  body{
    background: radial-gradient(circle at 10% 10%, #0b1220 0%, var(--bg2) 40%), linear-gradient(180deg, rgba(255,93,143,0.02), rgba(0,0,0,0.06));
    display:flex;align-items:center;justify-content:center;padding:28px;
  }
  .card{width:920px;max-width:96%;border-radius:18px;padding:28px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));box-shadow:0 18px 50px rgba(2,6,23,0.7);border:1px solid rgba(255,255,255,0.03);position:relative;overflow:hidden}
  .hearts{position:absolute;inset:0;pointer-events:none}
  .heart{position:absolute;width:18px;height:18px;transform:rotate(-45deg);opacity:0}
  .heart:before,.heart:after{content:"";position:absolute;width:18px;height:18px;border-radius:50%;background:linear-gradient(180deg,var(--accent),var(--accent-2))}
  .heart:before{top:-9px} .heart:after{left:9px}
  @keyframes floatUp{0%{opacity:0;transform:translateY(0) scale(0.6) rotate(-45deg)}8%{opacity:1;scale:1}100%{opacity:0;transform:translateY(-520px) scale(1.2) rotate(-45deg)}}
  .h1{left:6%;bottom:5%;animation:floatUp 6s linear infinite;animation-delay:-0.8s}
  .h2{left:18%;bottom:2%;animation:floatUp 5.6s linear infinite;animation-delay:-1.6s}
  .h3{left:32%;bottom:6%;animation:floatUp 6.4s linear infinite;animation-delay:-0.3s}
  .h4{left:48%;bottom:4%;animation:floatUp 5.8s linear infinite;animation-delay:-2s}
  .h5{left:64%;bottom:3%;animation:floatUp 6.2s linear infinite;animation-delay:-1.1s}
  .h6{left:80%;bottom:7%;animation:floatUp 5.4s linear infinite;animation-delay:-0.6s}
  .h7{left:90%;bottom:5%;animation:floatUp 6.6s linear infinite;animation-delay:-2.4s}

  .title{font-size:28px;margin:0 0 6px;color:var(--muted);letter-spacing:0.3px}
  .subtitle{margin:0 0 18px;color:rgba(230,238,248,0.7)}
  .row{display:flex;gap:18px;align-items:center;flex-wrap:wrap}
  .avatar{min-width:96px;width:96px;height:96px;border-radius:14px;background:linear-gradient(180deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;font-weight:700;color:#071028;font-size:22px;box-shadow:0 8px 30px rgba(255,93,143,0.12)}
  .poem{flex:1;padding:18px;border-radius:12px;background:linear-gradient(90deg, rgba(255,255,255,0.01), rgba(255,255,255,0.005));border:1px dashed rgba(255,93,143,0.06);line-height:1.65;font-size:18px;color:#f8f5f8;position:relative}
  .typewriter{font-weight:600;margin-bottom:12px;font-size:18px;color:var(--accent-2)}
  .typed{display:inline-block;border-right:2px solid rgba(255,255,255,0.25);padding-right:6px;animation:caret 1s steps(1,end) infinite}
  @keyframes caret{50%{border-color:transparent}}
  .controls{display:flex;gap:10px;margin-top:16px;align-items:center;flex-wrap:wrap}
  .btn{background:linear-gradient(90deg,var(--accent),var(--accent-2));border:none;padding:10px 14px;border-radius:12px;color:#071028;font-weight:600;cursor:pointer;box-shadow:0 8px 18px rgba(255,93,143,0.14)}
  .ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted);padding:9px 12px;border-radius:12px;cursor:pointer}
  .signature{margin-top:18px;color:rgba(230,238,248,0.85);font-style:italic}
  .big-heart{display:inline-block;margin-left:6px;font-size:22px;transform-origin:center}
  @media(max-width:820px){.row{flex-direction:column;align-items:flex-start}.avatar{width:86px;height:86px}.poem{font-size:16px}}
</style>
</head>
<body>
  <div class="card" role="article" aria-label="Romantic card">
    <div class="hearts" aria-hidden="true">
      <div class="heart h1"></div><div class="heart h2"></div><div class="heart h3"></div>
      <div class="heart h4"></div><div class="heart h5"></div><div class="heart h6"></div><div class="heart h7"></div>
    </div>

    <h1 class="title">Happy 4 months 💋🫶💘💓❤️‍🩹</h1>
    <p class="subtitle">For my Deha bbyy — my reason, my heart, my forever 💌</p>

    <div class="row">
      <div class="avatar">D♡</div>

      <div class="poem">
        <div class="typewriter">I kept a small corner of the world for you — <span class="typed" id="typed"></span></div>

        <p id="mainText">
          💫 <strong>Deha bbyy</strong> — when your eyes meet mine, I see galaxies blooming where my heart once felt empty. Your beauty, your softness, your presence… everything pulls me closer like destiny itself. 🌌💋
        </p>

        <p>Every breath of yours feels like prayer… Every silence like poetry… Every touch like a promise whispered by the universe itself. 🕊️✨</p>

        <p>Stay with me in this tiny universe we’ve built — two hearts learning, healing, choosing each other again and again. 🌙🫶</p>

        <div class="controls">
          <button class="btn" id="copyBtn">Copy Message</button>
          <button class="ghost" id="toggleBtn">Toggle Light</button>
          <button class="ghost" id="playBtn">Play / Pause Music</button>
        </div>

        <div class="signature">— Yours forever, <span id="fromName">Tumar bbyx</span> <span class="big-heart" id="pulseHeart">❤️</span></div>
      </div>
    </div>
  </div>

  <!-- Royalty-free soft piano from Pixabay (autoplay may be blocked on some browsers until user interaction) -->
  <audio id="bgAudio" loop preload="none">
    <source src="https://cdn.pixabay.com/download/audio/2023/02/28/audio_9a8980e63d.mp3?filename=romantic-piano-141403.mp3" type="audio/mpeg">
    Your browser does not support the audio element.
  </audio>

<script>
  const phrases = ["my forever.","my quiet prayer.","the dream I keep living.","my sweetest devotion."];
  let idx = 0; const typedEl = document.getElementById('typed');
  function typeLoop(){ const text = phrases[idx % phrases.length]; let i = 0; typedEl.textContent = ''; const typer = setInterval(()=>{ typedEl.textContent += text.charAt(i); i++; if(i > text.length){ clearInterval(typer); setTimeout(()=>eraseLoop(), 1300); } }, 80); }
  function eraseLoop(){ let s = typedEl.textContent; let i = s.length; const eraser = setInterval(()=>{ typedEl.textContent = s.substring(0, i-1); i--; if(i <= 0){ clearInterval(eraser); idx++; setTimeout(typeLoop, 500); } },40); }
  typeLoop();

  // copy poem text
  const copyBtn = document.getElementById('copyBtn');
  const mainText = document.getElementById('mainText');
  copyBtn.addEventListener('click', async ()=>{
    const finalText = mainText.innerText + "\n\n— " + document.getElementById('fromName').innerText;
    try{ await navigator.clipboard.writeText(finalText); copyBtn.textContent = 'Copied!'; setTimeout(()=>copyBtn.textContent='Copy Message',1500); }catch{ alert("Copy not supported — select and copy manually."); }
  });

  // toggle brightness
  document.getElementById('toggleBtn').addEventListener('click', ()=>{
    document.body.style.filter = document.body.style.filter === 'brightness(1.05)' ? '' : 'brightness(1.05)';
  });

  // audio controls & autoplay attempt
  const audio = document.getElementById('bgAudio');
  const playBtn = document.getElementById('playBtn');
  playBtn.addEventListener('click', async ()=>{
    try{
      if(audio.paused){ await audio.play(); playBtn.textContent = 'Pause Music'; } else { audio.pause(); playBtn.textContent = 'Play / Pause Music'; }
    }catch(e){
      alert('Autoplay blocked — please tap Play to start music.');
    }
  });

  // gentle heart pulse
  const pulseHeart = document.getElementById('pulseHeart');
  setInterval(()=>{ pulseHeart.style.transform='scale(1.08)'; setTimeout(()=>pulseHeart.style.transform='scale(1)',300); },1500);
</script>
</body>
</html>
